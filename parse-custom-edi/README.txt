This project reads a PRICAT edi file using the bindings distributed with Smooks.

The first passage reacts to the events by applying a template. The second passage creates a Java bean from the input data and persists it to the database.
